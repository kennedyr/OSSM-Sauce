extends Node

enum Command {
  RESPONSE,
  MOVE,
  LOOP,
  POSITION,
  VIBRATE,
  PLAY,
  PAUSE,
  RESET,
  HOMING,
  CONNECTION,
  SET_SPEED_LIMIT,
  SET_GLOBAL_ACCELERATION,
  SET_RANGE_LIMIT,
  SET_HOMING_SPEED,
  SET_HOMING_TRIGGER,
  SMOOTH_MOVE,
}


enum RangeLimitType {
  MIN_RANGE,
  MAX_RANGE,
}


enum TransType {
  LINEAR,
  SINE,
  CIRC,
  EXPO,
  QUAD,
  CUBIC,
  QUART,
  QUINT
}
